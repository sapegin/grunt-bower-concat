/* Author: Artem Sapegin, http://sapegin.me, 2013 */

/*global utils:false */
;(function ($) {
	'use strict';

	utils.initComponents({
		test: function(elem) {
			$(elem).html('Grunt is awesome!');
		}
	});

}(jQuery));
